{
  "name": "hardhat-project",
  "scripts": {
    "install": "npm install --save-dev @openzeppelin/test-helpers && npm install --save-dev hardhat && npm install --save-dev @openzeppelin/contracts",
    "test": "REPORT_GAS=true npx hardhat test"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^1.0.2",
    "@openzeppelin/contracts": "^4.7.3",
    "@openzeppelin/test-helpers": "^0.5.15",
    "hardhat": "^2.10.2"
  }
}
